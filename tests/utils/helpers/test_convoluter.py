import unittest

from hypothesis import given
from hypothesis import strategies as st

from utils.helpers.convoluter import get_conv_sign

class TestConvoluter(unittest.TestCase):
  pass

  def test_get_conv_sign(self, inp_shape, out_shape):
    if the shapes are not convolvable
      with self.assertRaises()
    if the input matrix is larger than the output matrix
    elif the input matrix is smaller than the output matrix
    else
if __name__ == '__main__':
  unittest.main()